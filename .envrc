# direnv: auto-loads .env when you cd into this repo. Run `direnv allow` once.
# Keeps ScratchPad's signing key scoped to this project so it never clobbers
# other Tauri apps' builds.
dotenv
# dotenv drops empty values, so export the empty key-password explicitly.
export TAURI_SIGNING_PRIVATE_KEY_PASSWORD=""
